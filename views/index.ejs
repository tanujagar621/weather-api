<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>My Weather</title>
</head>
<body>
    <div class="container">
        <header>
            <div class="icon">
                <img src="/icons/compass.svg" alt="logo">
            </div>
        </header>
        <div class="title">
            <h1>My Weather</h1>
            <p>Check the current weather for your city</p>
        </div>
        <form action="/" method="post">
            <input type="text" name="city" class="input-city" placeholder="Enter Name Of Your City">
            <!-- <button type="submit">Search</button> -->
        </form>
        <div class="result">
            <% if(temperature !== null) {%>
                <div class="temperature"><h1><%= (temperature-273.15).toFixed(2) %> <span class="degree">o</span> </h1></div>
            <% } if(icon !== null) {%>
            <div class="icon-des">
                <img class="weather-icon" src="http://openweathermap.org/img/w/<%= icon %>.png" alt="">
                <% } if(desc !==null) {%>
                <h3 class="desc"><%= desc %></h3>
            </div>
            <% } if(city !==null) {%>
            <div class="city"><%= city %></div>
            <% } %>
        </div>
    </div>
</body>
</html>